// reduce() in JS

let numbers = [12, 33, 32, 67];

let total = numbers.reduce((a, b) => a + b, 0) // It can solve any problem where you want to combine / transform / compress an array into a single output and take pair of values as e argument by default

console.log(total);